---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

const lang = 'ru';

// Mock-данные для альбома "Концерт в Португалии"
const album = {
  title: 'Концерт в Португалии',
  date: 'Ноябрь 2025',
  description: 'Фотографии с концерта в Лиссабоне. Специальная программа с португальскими музыкантами.',
  type: 'photo',
};

// 15 фото — используем существующие изображения проекта
const photos = [
  { id: '1', src: '/projects/klassika-poster.jpg', selected: false },
  { id: '2', src: '/projects/klassika-1.jpg', selected: true },
  { id: '3', src: '/projects/klassika-2.jpg', selected: false },
  { id: '4', src: '/projects/ogonek-2024-1.jpg', selected: true },
  { id: '5', src: '/projects/ogonek-2024-2.jpg', selected: false },
  { id: '6', src: '/projects/holodok-2025-1.png', selected: true },
  { id: '7', src: '/projects/holodok-2025-2.png', selected: false },
  { id: '8', src: '/projects/holodok-2025-3.png', selected: false },
  { id: '9', src: '/hero/VGD_1998.jpg', selected: false },
  { id: '10', src: '/hero/VGD_2151.jpg', selected: true },
  { id: '11', src: '/hero/VGD_3073.jpg', selected: false },
  { id: '12', src: '/hero/VGD_3241.jpg', selected: false },
  { id: '13', src: '/musicians/rakitin-1.jpg', selected: false },
  { id: '14', src: '/musicians/slezkin.jpg', selected: false },
  { id: '15', src: '/musicians/kotnov-1.jpg', selected: false },
];

const selectedCount = photos.filter(p => p.selected).length;
---

<Layout title={`${album.title} — Медиа`} lang={lang}>
  <Header currentLang={lang} />
  
  <main class="pt-24 pb-20">
    <div class="fixed top-20 right-4 z-50 bg-amber-600/80 text-white px-3 py-1 text-xs font-medium rounded">
      ДЕМО: Страница альбома
    </div>

    <div class="max-w-6xl mx-auto px-6">
      
      <!-- Навигация назад -->
      <a href="/_demo/media-albums" class="inline-flex items-center gap-2 text-muted hover:text-white transition-colors mb-8">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
        Все альбомы
      </a>
      
      <!-- Заголовок альбома -->
      <div class="mb-8">
        <h1 class="text-3xl md:text-4xl font-light tracking-wide mb-2" style="font-family: var(--font-logo);">
          {album.title}
        </h1>
        <p class="text-muted">
          {album.date} · {photos.length} фото
        </p>
        {album.description && (
          <p class="text-white/60 text-sm mt-4 max-w-2xl">
            {album.description}
          </p>
        )}
      </div>

      <!-- Панель режима выбора -->
      <div class="flex items-center justify-between mb-6 p-4 border border-white/10 bg-white/5">
        <div class="flex items-center gap-4">
          <button 
            id="toggle-select-mode"
            class="text-sm text-white/60 hover:text-white transition-colors flex items-center gap-2"
          >
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            Режим выбора
          </button>
          <span class="text-white/30">|</span>
          <span class="text-sm text-white/40" id="selected-count">
            Выбрано: <span class="text-white">{selectedCount}</span> из {photos.length}
          </span>
        </div>
        
        <div class="flex items-center gap-2">
          <button class="text-xs text-white/40 hover:text-white transition-colors px-3 py-1 border border-white/20 hover:border-white/40">
            Выбрать все
          </button>
          <button class="text-xs text-white/40 hover:text-white transition-colors px-3 py-1 border border-white/20 hover:border-white/40">
            Снять выбор
          </button>
        </div>
      </div>

      <!-- Сетка фото -->
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-3" id="photos-grid">
        {photos.map((photo, index) => (
          <div 
            class={`photo-item group relative aspect-square overflow-hidden cursor-pointer border-2 transition-all ${photo.selected ? 'border-teal-500' : 'border-transparent hover:border-white/30'}`}
            data-id={photo.id}
            data-index={index}
            data-src={photo.src}
          >
            <img 
              src={photo.src} 
              alt={`Фото ${index + 1}`}
              class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
              loading="lazy"
            />
            
            <!-- Оверлей при наведении -->
            <div class="absolute inset-0 bg-black/0 group-hover:bg-black/30 transition-all flex items-center justify-center opacity-0 group-hover:opacity-100">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0zM10 7v3m0 0v3m0-3h3m-3 0H7" />
              </svg>
            </div>
            
            <!-- Чекбокс выбора (Вариант 1: в углу) -->
            <div 
              class={`select-checkbox absolute top-2 left-2 w-6 h-6 rounded-full border-2 flex items-center justify-center transition-all ${photo.selected ? 'bg-teal-500 border-teal-500' : 'border-white/50 bg-black/30 opacity-0 group-hover:opacity-100'}`}
            >
              {photo.selected && (
                <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7" />
                </svg>
              )}
            </div>
            
            <!-- Номер фото -->
            <div class="absolute bottom-2 right-2 text-white/50 text-xs opacity-0 group-hover:opacity-100 transition-opacity">
              {index + 1}
            </div>
          </div>
        ))}
      </div>

      <!-- Вопросы для обсуждения -->
      <section class="mt-16 p-6 border border-white/10 bg-white/5">
        <h2 class="text-white text-lg mb-4">❓ Вопросы для обсуждения</h2>
        <ul class="text-muted text-sm space-y-2">
          <li>• <strong>Lightbox:</strong> При клике открывать фото на весь экран со стрелками?</li>
          <li>• <strong>Режим выбора:</strong> Галочки всегда видны или только в режиме выбора?</li>
          <li>• <strong>Пагинация:</strong> Infinite scroll или страницы?</li>
          <li>• <strong>Скачивание:</strong> Нужна кнопка скачать выбранные?</li>
          <li>• <strong>Сортировка:</strong> По дате? По имени? Ручная?</li>
        </ul>
      </section>

      <!-- Варианты UI выбора -->
      <section class="mt-8 p-6 border border-white/10 bg-white/5">
        <h2 class="text-white text-lg mb-4">🎨 Варианты UI выбора фото</h2>
        
        <div class="grid md:grid-cols-3 gap-6 mt-6">
          <!-- Вариант 1 -->
          <div class="text-center">
            <div class="aspect-square bg-white/10 mb-3 relative">
              <div class="absolute top-2 left-2 w-6 h-6 rounded-full bg-teal-500 flex items-center justify-center">
                <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7" />
                </svg>
              </div>
            </div>
            <p class="text-white text-sm">Вариант 1</p>
            <p class="text-white/50 text-xs">Круглый чекбокс в углу</p>
          </div>
          
          <!-- Вариант 2 -->
          <div class="text-center">
            <div class="aspect-square bg-white/10 mb-3 relative border-4 border-teal-500">
            </div>
            <p class="text-white text-sm">Вариант 2</p>
            <p class="text-white/50 text-xs">Цветная рамка выбранного</p>
          </div>
          
          <!-- Вариант 3 -->
          <div class="text-center">
            <div class="aspect-square bg-white/10 mb-3 relative">
              <div class="absolute inset-0 bg-teal-500/30 flex items-center justify-center">
                <svg class="w-12 h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
              </div>
            </div>
            <p class="text-white text-sm">Вариант 3</p>
            <p class="text-white/50 text-xs">Оверлей + большая галочка</p>
          </div>
        </div>
      </section>

    </div>
  </main>
  
  <Footer />

  <!-- Lightbox -->
  <div id="lightbox" class="fixed inset-0 z-50 bg-black/95 hidden items-center justify-center">
    <button id="lightbox-close" class="absolute top-6 right-6 text-white/60 hover:text-white transition-colors">
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
    
    <button id="lightbox-prev" class="absolute left-6 top-1/2 -translate-y-1/2 text-white/60 hover:text-white transition-colors">
      <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
      </svg>
    </button>
    
    <button id="lightbox-next" class="absolute right-6 top-1/2 -translate-y-1/2 text-white/60 hover:text-white transition-colors">
      <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
      </svg>
    </button>
    
    <img id="lightbox-img" src="" alt="" class="max-w-[90vw] max-h-[90vh] object-contain" />
    
    <div id="lightbox-counter" class="absolute bottom-6 left-1/2 -translate-x-1/2 text-white/60 text-sm">
      1 / 15
    </div>
  </div>
</Layout>

<style>
  a {
    text-decoration: none !important;
  }
  
  #lightbox.active {
    display: flex;
  }
</style>

<script>
  // Lightbox
  const lightbox = document.getElementById('lightbox');
  const lightboxImg = document.getElementById('lightbox-img') as HTMLImageElement;
  const lightboxCounter = document.getElementById('lightbox-counter');
  const lightboxClose = document.getElementById('lightbox-close');
  const lightboxPrev = document.getElementById('lightbox-prev');
  const lightboxNext = document.getElementById('lightbox-next');
  const photoItems = document.querySelectorAll('.photo-item');
  
  let currentIndex = 0;
  const photos = Array.from(photoItems).map(item => ({
    src: item.dataset.src,
    index: parseInt(item.dataset.index || '0')
  }));
  
  function openLightbox(index: number) {
    currentIndex = index;
    updateLightbox();
    lightbox?.classList.add('active');
    document.body.style.overflow = 'hidden';
  }
  
  function closeLightbox() {
    lightbox?.classList.remove('active');
    document.body.style.overflow = '';
  }
  
  function updateLightbox() {
    if (lightboxImg && photos[currentIndex]) {
      lightboxImg.src = photos[currentIndex].src || '';
    }
    if (lightboxCounter) {
      lightboxCounter.textContent = `${currentIndex + 1} / ${photos.length}`;
    }
  }
  
  function nextPhoto() {
    currentIndex = (currentIndex + 1) % photos.length;
    updateLightbox();
  }
  
  function prevPhoto() {
    currentIndex = (currentIndex - 1 + photos.length) % photos.length;
    updateLightbox();
  }
  
  // Открытие по клику на фото
  photoItems.forEach((item, index) => {
    item.addEventListener('click', (e) => {
      // Не открываем lightbox если кликнули на чекбокс
      if ((e.target as HTMLElement).closest('.select-checkbox')) return;
      openLightbox(index);
    });
  });
  
  lightboxClose?.addEventListener('click', closeLightbox);
  lightboxPrev?.addEventListener('click', prevPhoto);
  lightboxNext?.addEventListener('click', nextPhoto);
  
  // Клавиши
  document.addEventListener('keydown', (e) => {
    if (!lightbox?.classList.contains('active')) return;
    
    if (e.key === 'Escape') closeLightbox();
    if (e.key === 'ArrowRight') nextPhoto();
    if (e.key === 'ArrowLeft') prevPhoto();
  });
  
  // Клик по фону закрывает
  lightbox?.addEventListener('click', (e) => {
    if (e.target === lightbox) closeLightbox();
  });
  
  // Выбор фото (toggle)
  document.querySelectorAll('.select-checkbox').forEach(checkbox => {
    checkbox.addEventListener('click', (e) => {
      e.stopPropagation();
      const item = (e.target as HTMLElement).closest('.photo-item');
      const isSelected = item?.classList.contains('border-teal-500');
      
      if (isSelected) {
        item?.classList.remove('border-teal-500');
        item?.classList.add('border-transparent');
        checkbox.classList.remove('bg-teal-500', 'border-teal-500');
        checkbox.classList.add('border-white/50', 'bg-black/30');
        checkbox.innerHTML = '';
      } else {
        item?.classList.add('border-teal-500');
        item?.classList.remove('border-transparent');
        checkbox.classList.add('bg-teal-500', 'border-teal-500');
        checkbox.classList.remove('border-white/50', 'bg-black/30');
        checkbox.innerHTML = '<svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7" /></svg>';
      }
      
      // Обновляем счётчик
      const selectedCount = document.querySelectorAll('.photo-item.border-teal-500').length;
      const countEl = document.querySelector('#selected-count span');
      if (countEl) countEl.textContent = String(selectedCount);
    });
  });
</script>


