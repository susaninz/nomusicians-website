---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';

const lang = 'ru';

// Примеры фото для галереи (используем существующие)
const galleryPhotos = [
  { src: '/hero.jpg', alt: 'Выступление 1' },
  { src: '/history/band-early.jpg', alt: 'Ранние годы' },
  { src: '/projects/camp-klassika.jpg', alt: 'Кэмп Классика' },
  { src: '/projects/lampu-bali.jpg', alt: 'Lampu Bali' },
  { src: '/releases/ogonek-23.jpg', alt: 'Огонёк 2023' },
  { src: '/releases/qatar-22.jpg', alt: 'Qatar 2022' },
  { src: '/releases/blanc-19.jpg', alt: 'Blanc 2019' },
  { src: '/releases/jam-loft-23.jpg', alt: 'Jam Loft 2023' },
];
---

<Layout title="Демо: Варианты галереи — Nomusicians">
  <Header currentLang={lang} />
  
  <main class="pt-24 pb-16">
    <!-- Заголовок -->
    <section class="px-6 mb-16">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-4xl md:text-5xl font-light tracking-wide mb-6" style="font-family: var(--font-logo);">
          Варианты галереи
        </h1>
        <p class="text-lg text-muted">
          Два варианта отображения фото: горизонтальный скролл и сетка с лайтбоксом
        </p>
      </div>
    </section>

    <!-- Вариант A: Горизонтальный скролл -->
    <section class="mb-24">
      <div class="max-w-6xl mx-auto px-6 mb-8">
        <h2 class="text-2xl font-light tracking-wide mb-2">Вариант A: Горизонтальный скролл</h2>
        <p class="text-muted text-sm">Свайп влево-вправо. Хорош для мобильных устройств.</p>
      </div>
      
      <!-- Галерея скролл -->
      <div class="gallery-scroll overflow-x-auto scrollbar-hide">
        <div class="flex gap-4 px-6 md:px-12 w-max">
          {galleryPhotos.map((photo, index) => (
            <div 
              class="flex-shrink-0 w-72 md:w-96 aspect-square overflow-hidden group cursor-pointer"
              data-lightbox-trigger
              data-index={index}
            >
              <img 
                src={photo.src} 
                alt={photo.alt}
                class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-500 group-hover:scale-105"
                loading="lazy"
              />
            </div>
          ))}
        </div>
      </div>
      
      <!-- Индикаторы N8 (штили/фейдеры) -->
      <div class="flex justify-center items-end gap-5 mt-8 h-10">
        {galleryPhotos.map((_, index) => {
          const heights = [12, 20, 16, 24, 14, 18, 22, 10];
          const h = heights[index % heights.length];
          return (
            <button 
              class="scroll-dot indicator-n8"
              data-dot-index={index}
              style={`--stem-height: ${h}px`}
              aria-label={`Фото ${index + 1}`}
            ></button>
          );
        })}
      </div>
    </section>

    <!-- Разделитель -->
    <div class="max-w-4xl mx-auto px-6 mb-24">
      <hr class="border-white/10" />
    </div>

    <!-- Вариант B: Сетка с лайтбоксом -->
    <section class="mb-24 px-6">
      <div class="max-w-6xl mx-auto mb-8">
        <h2 class="text-2xl font-light tracking-wide mb-2">Вариант B: Сетка с лайтбоксом</h2>
        <p class="text-muted text-sm">Классическая сетка. При клике открывается на весь экран. Удобно для десктопа.</p>
      </div>
      
      <!-- Галерея сетка -->
      <div class="max-w-6xl mx-auto grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-3 md:gap-4">
        {galleryPhotos.map((photo, index) => (
          <div 
            class="aspect-square overflow-hidden group cursor-pointer"
            data-lightbox-trigger-grid
            data-index={index}
          >
            <img 
              src={photo.src} 
              alt={photo.alt}
              class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-500 group-hover:scale-105"
              loading="lazy"
            />
          </div>
        ))}
      </div>
    </section>

    <!-- Лайтбокс (общий для обоих вариантов) -->
    <div 
      id="lightbox" 
      class="fixed inset-0 z-50 bg-black/95 hidden items-center justify-center"
    >
      <button 
        id="lightbox-close"
        class="absolute top-6 right-6 text-white/50 hover:text-white transition-colors z-10"
      >
        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>
      
      <button 
        id="lightbox-prev"
        class="absolute left-4 top-1/2 -translate-y-1/2 text-white/50 hover:text-white transition-colors z-10"
      >
        <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 19l-7-7 7-7"></path>
        </svg>
      </button>
      
      <button 
        id="lightbox-next"
        class="absolute right-4 top-1/2 -translate-y-1/2 text-white/50 hover:text-white transition-colors z-10"
      >
        <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5l7 7-7 7"></path>
        </svg>
      </button>
      
      <img 
        id="lightbox-image" 
        src="" 
        alt=""
        class="max-w-[90vw] max-h-[85vh] object-contain"
      />
      
      <p id="lightbox-caption" class="absolute bottom-6 left-1/2 -translate-x-1/2 text-white/70 text-sm"></p>
    </div>

    <!-- Сравнение -->
    <section class="px-6 py-16 bg-surface/30">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-2xl font-light tracking-wide text-center mb-12">Сравнение</h2>
        
        <div class="grid md:grid-cols-2 gap-8">
          <div class="p-6 border border-white/10 rounded-lg">
            <h3 class="text-lg font-light mb-4">Горизонтальный скролл</h3>
            <ul class="space-y-2 text-sm text-muted">
              <li class="flex items-center gap-2">
                <span class="text-green-500">✓</span> Привычен пользователям соцсетей
              </li>
              <li class="flex items-center gap-2">
                <span class="text-green-500">✓</span> Хорошо работает на мобильных
              </li>
              <li class="flex items-center gap-2">
                <span class="text-green-500">✓</span> Фокус на одном фото
              </li>
              <li class="flex items-center gap-2">
                <span class="text-yellow-500">~</span> На десктопе менее интуитивен
              </li>
            </ul>
          </div>
          
          <div class="p-6 border border-white/10 rounded-lg">
            <h3 class="text-lg font-light mb-4">Сетка с лайтбоксом</h3>
            <ul class="space-y-2 text-sm text-muted">
              <li class="flex items-center gap-2">
                <span class="text-green-500">✓</span> Видно всё сразу
              </li>
              <li class="flex items-center gap-2">
                <span class="text-green-500">✓</span> Классика, знакомо всем
              </li>
              <li class="flex items-center gap-2">
                <span class="text-green-500">✓</span> Хорошо на десктопе
              </li>
              <li class="flex items-center gap-2">
                <span class="text-yellow-500">~</span> Маленькие превью на мобильных
              </li>
            </ul>
          </div>
        </div>
        
        <p class="text-center text-muted mt-8">
          💡 Можно использовать оба: скролл для "историй" проекта, сетку для галереи фото
        </p>
      </div>
    </section>

    <!-- Навигация -->
    <section class="px-6 py-16 text-center">
      <a href="/" class="text-white/60 hover:text-white transition-colors">
        ← Вернуться на главную
      </a>
    </section>
  </main>
</Layout>

<style>
  /* Скрыть скроллбар */
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  
  /* Индикаторы N8 (штили/фейдеры) через псевдоэлементы */
  .indicator-n8 {
    position: relative;
    width: 8px;
    cursor: pointer;
    display: flex;
    flex-direction: column;
    align-items: center;
    background: none;
    border: none;
    padding: 0;
  }
  
  .indicator-n8::before {
    content: '';
    display: block;
    width: 1px;
    height: var(--stem-height, 16px);
    background: rgba(255, 255, 255, 0.2);
    transition: all 0.3s ease;
  }
  
  .indicator-n8::after {
    content: '';
    display: block;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.3);
    margin-top: -2px;
    transition: all 0.3s ease;
  }
  
  .indicator-n8.active::before,
  .indicator-n8:hover::before {
    background: white;
  }
  
  .indicator-n8.active::after,
  .indicator-n8:hover::after {
    background: white;
  }
</style>

<script>
  // Данные фото для лайтбокса
  const photos = [
    { src: '/hero.jpg', alt: 'Выступление 1' },
    { src: '/history/band-early.jpg', alt: 'Ранние годы' },
    { src: '/projects/camp-klassika.jpg', alt: 'Кэмп Классика' },
    { src: '/projects/lampu-bali.jpg', alt: 'Lampu Bali' },
    { src: '/releases/ogonek-23.jpg', alt: 'Огонёк 2023' },
    { src: '/releases/qatar-22.jpg', alt: 'Qatar 2022' },
    { src: '/releases/blanc-19.jpg', alt: 'Blanc 2019' },
    { src: '/releases/jam-loft-23.jpg', alt: 'Jam Loft 2023' },
  ];

  let currentIndex = 0;
  const lightbox = document.getElementById('lightbox');
  const lightboxImage = document.getElementById('lightbox-image') as HTMLImageElement;
  const lightboxCaption = document.getElementById('lightbox-caption');
  const closeBtn = document.getElementById('lightbox-close');
  const prevBtn = document.getElementById('lightbox-prev');
  const nextBtn = document.getElementById('lightbox-next');

  function openLightbox(index: number) {
    currentIndex = index;
    lightboxImage.src = photos[index].src;
    lightboxCaption!.textContent = photos[index].alt;
    lightbox?.classList.remove('hidden');
    lightbox?.classList.add('flex');
    document.body.style.overflow = 'hidden';
  }

  function closeLightbox() {
    lightbox?.classList.add('hidden');
    lightbox?.classList.remove('flex');
    document.body.style.overflow = '';
  }

  function showPrev() {
    currentIndex = (currentIndex - 1 + photos.length) % photos.length;
    lightboxImage.src = photos[currentIndex].src;
    lightboxCaption!.textContent = photos[currentIndex].alt;
  }

  function showNext() {
    currentIndex = (currentIndex + 1) % photos.length;
    lightboxImage.src = photos[currentIndex].src;
    lightboxCaption!.textContent = photos[currentIndex].alt;
  }

  // Event listeners
  document.querySelectorAll('[data-lightbox-trigger], [data-lightbox-trigger-grid]').forEach(el => {
    el.addEventListener('click', () => {
      const index = parseInt(el.getAttribute('data-index') || '0');
      openLightbox(index);
    });
  });

  closeBtn?.addEventListener('click', closeLightbox);
  prevBtn?.addEventListener('click', showPrev);
  nextBtn?.addEventListener('click', showNext);

  lightbox?.addEventListener('click', (e) => {
    if (e.target === lightbox) closeLightbox();
  });

  document.addEventListener('keydown', (e) => {
    if (lightbox?.classList.contains('hidden')) return;
    if (e.key === 'Escape') closeLightbox();
    if (e.key === 'ArrowLeft') showPrev();
    if (e.key === 'ArrowRight') showNext();
  });

  // Горизонтальный скролл: индикаторы N8 (штили/фейдеры)
  const scrollContainer = document.querySelector('.gallery-scroll');
  const dots = document.querySelectorAll('.scroll-dot');
  
  function updateIndicators(activeIndex: number) {
    dots.forEach((dot, i) => {
      if (i === activeIndex) {
        dot.classList.add('active');
      } else {
        dot.classList.remove('active');
      }
    });
  }
  
  // Инициализируем первый индикатор как активный
  updateIndicators(0);
  
  scrollContainer?.addEventListener('scroll', () => {
    const scrollLeft = scrollContainer.scrollLeft;
    const itemWidth = 384 + 16; // w-96 + gap-4
    const activeIndex = Math.round(scrollLeft / itemWidth);
    updateIndicators(activeIndex);
  });

  // Клик на dots для навигации
  dots.forEach((dot, index) => {
    dot.addEventListener('click', () => {
      const itemWidth = 384 + 16;
      scrollContainer?.scrollTo({
        left: index * itemWidth,
        behavior: 'smooth'
      });
    });
  });
</script>

