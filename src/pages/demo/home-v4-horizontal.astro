---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';

const lang = 'ru';

// Демо данные - timeline событий
const events = [
  {
    type: 'concert',
    title: 'Концерт в Лиссабоне',
    subtitle: 'Casa da Música',
    date: '15 декабря 2025',
    dateShort: '15 дек',
    buttonText: 'Билеты',
    buttonUrl: '#',
    isFuture: true,
    isPinned: true,
  },
  {
    type: 'release',
    title: 'Noch 3.0',
    subtitle: 'Новый альбом',
    date: '1 декабря 2025',
    dateShort: '1 дек',
    buttonText: 'Слушать',
    buttonUrl: '#',
    isFuture: false,
    isPinned: false,
  },
  {
    type: 'news',
    title: 'Интервью для Афиши',
    subtitle: 'О планах на 2026',
    date: '20 ноября 2025',
    dateShort: '20 ноя',
    buttonText: 'Читать',
    buttonUrl: '#',
    isFuture: false,
    isPinned: false,
  },
  {
    type: 'concert',
    title: 'Концерт в Москве',
    subtitle: 'ДК Рассвет',
    date: '15 ноября 2025',
    dateShort: '15 ноя',
    buttonText: 'Смотреть',
    buttonUrl: '#',
    isFuture: false,
    isPinned: false,
  },
  {
    type: 'release',
    title: 'Live at Ogonek',
    subtitle: 'Концертная запись',
    date: '1 октября 2025',
    dateShort: '1 окт',
    buttonText: 'Слушать',
    buttonUrl: '#',
    isFuture: false,
    isPinned: false,
  },
];

const getTypeIcon = (type: string) => {
  switch(type) {
    case 'concert': return '🎫';
    case 'release': return '🎵';
    case 'news': return '📰';
    default: return '•';
  }
};

const getTypeColor = (type: string) => {
  switch(type) {
    case 'concert': return 'text-amber-400';
    case 'release': return 'text-green-400';
    case 'news': return 'text-blue-400';
    default: return 'text-white';
  }
};

const getTypeLabel = (type: string) => {
  switch(type) {
    case 'concert': return 'Концерт';
    case 'release': return 'Релиз';
    case 'news': return 'Новость';
    default: return '';
  }
};
---

<Layout title="Главная — Горизонтальный Timeline" lang={lang}>
  <Header currentLang={lang} />
  
  <main>
    <!-- Демо лейбл -->
    <div class="fixed top-20 right-4 z-50 bg-orange-500/80 text-white px-3 py-1 text-xs font-medium rounded">
      DEMO: События (горизонтальный)
    </div>

    <!-- Hero -->
    <section class="relative h-screen flex items-center justify-center overflow-hidden">
      <div class="absolute inset-0 bg-gradient-to-b from-black/60 via-black/40 to-black"></div>
      <img 
        src="/hero.jpg" 
        alt="Nomusicians" 
        class="absolute inset-0 w-full h-full object-cover -z-10"
      />
      <div class="relative z-10 text-center px-6">
        <h1 class="text-5xl md:text-7xl lg:text-8xl font-light tracking-wider mb-4" style="font-family: var(--font-logo);">
          Nomusicians
        </h1>
        <p class="text-lg md:text-xl text-muted tracking-wide">
          Электроника и неоклассика
        </p>
      </div>
    </section>

    <!-- События — горизонтальный timeline -->
    <section class="py-20 px-6">
      <div class="max-w-6xl mx-auto">
        <h2 class="text-sm font-light tracking-widest uppercase text-white mb-12 text-center">
          События
        </h2>
        
        <!-- Горизонтальный скролл -->
        <div class="relative">
          <!-- Горизонтальная линия -->
          <div class="absolute left-0 right-0 top-[11px] h-px bg-white/20"></div>
          
          <div class="overflow-x-auto pb-4 -mx-6 px-6 hide-scrollbar">
            <div class="flex gap-8 min-w-max">
              {events.map((event) => (
                <a 
                  href={event.buttonUrl} 
                  class={`group block no-underline w-56 flex-shrink-0 ${event.isFuture ? 'opacity-100' : 'opacity-60 hover:opacity-100'} transition-opacity`}
                >
                  <!-- Точка + дата -->
                  <div class="flex items-center gap-3 mb-4">
                    <div class={`w-[22px] h-[22px] rounded-full border-2 ${event.isFuture ? 'border-white bg-white' : 'border-white/40 bg-transparent'} flex items-center justify-center`}>
                      {event.isFuture && <div class="w-2 h-2 rounded-full bg-black"></div>}
                    </div>
                    <span class="text-sm text-muted font-mono">{event.dateShort}</span>
                  </div>
                  
                  <!-- Контент -->
                  <div class="pl-1">
                    <div class={`text-xs uppercase tracking-wider ${getTypeColor(event.type)} mb-2`}>
                      {getTypeIcon(event.type)} {getTypeLabel(event.type)}
                    </div>
                    <h3 class="text-lg font-light text-white group-hover:text-muted transition-colors mb-1">
                      {event.title}
                    </h3>
                    <p class="text-muted text-sm mb-3">{event.subtitle}</p>
                    <span class="text-xs text-white/50 group-hover:text-white transition-colors flex items-center gap-1">
                      {event.buttonText}
                      <svg class="w-3 h-3 transform group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                      </svg>
                    </span>
                  </div>
                </a>
              ))}
            </div>
          </div>
          
          <!-- Градиенты по краям для индикации скролла -->
          <div class="absolute left-0 top-0 bottom-0 w-12 bg-gradient-to-r from-black to-transparent pointer-events-none"></div>
          <div class="absolute right-0 top-0 bottom-0 w-12 bg-gradient-to-l from-black to-transparent pointer-events-none"></div>
        </div>
        
        <!-- Все события -->
        <div class="mt-8 text-center">
          <a href="#" class="text-muted hover:text-white transition-colors text-sm">
            Все события →
          </a>
        </div>
      </div>
    </section>

    <!-- Видео секция -->
    <section class="py-20 px-6">
      <div class="max-w-6xl mx-auto text-center">
        <h2 class="text-2xl font-light tracking-wide mb-8" style="font-family: var(--font-logo);">
          Видео
        </h2>
        <div class="aspect-video bg-surface/50 flex items-center justify-center text-muted">
          [Видео блок]
        </div>
      </div>
    </section>

  </main>
</Layout>

<style>
  a {
    text-decoration: none !important;
  }
  
  /* Скрываем скроллбар но оставляем функционал */
  .hide-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  .hide-scrollbar::-webkit-scrollbar {
    display: none;
  }
</style>

