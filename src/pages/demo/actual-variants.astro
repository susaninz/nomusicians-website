---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';

const lang = 'ru';

// Демо данные
const concert = {
  type: 'concert',
  title: 'Концерт в Лиссабоне',
  description: 'Выступление в легендарном зале Casa da Música. Программа «Classitronics» — электроника и неоклассика.',
  date: '15 декабря 2025',
  time: '20:00',
  buttonText: 'Билеты',
  buttonUrl: '#',
};

const release = {
  type: 'release',
  title: 'Noch 3.0',
  description: 'Третья часть студийного альбома. Атмосферная электроника, тяготеющая к камерной музыке.',
  date: '1 декабря 2025',
  buttonText: 'Слушать',
  buttonUrl: '#',
};

const news = {
  type: 'news',
  title: 'Интервью для Афиши',
  description: 'Разговор о новом альбоме, концертах в Европе и планах на 2026 год.',
  date: '20 ноября 2025',
  buttonText: 'Читать',
  buttonUrl: '#',
};
---

<Layout title="Варианты блока Актуальное" lang={lang}>
  <Header currentLang={lang} />
  
  <main class="pt-24 pb-20">
    <div class="max-w-4xl mx-auto px-6">
      
      <h1 class="text-3xl font-light text-center mb-4">Варианты блока «Актуальное»</h1>
      <p class="text-muted text-center mb-16">Концерт выделен, остальные события ниже</p>

      <!-- ========== ВАРИАНТ A ========== -->
      <section class="mb-20">
        <div class="text-xs text-amber-400 mb-4 tracking-widest uppercase">Вариант A — Концерт крупно сверху</div>
        
        <div class="bg-surface py-12 px-6 -mx-6">
          <p class="text-muted text-sm tracking-[0.2em] uppercase mb-8 text-center">Актуальное</p>
          
          <!-- Концерт — большая карточка -->
          <a href="#" class="group block mb-6 p-6 border border-white/20 hover:border-white/40 hover:bg-white/5 transition-all no-underline">
            <div class="flex items-start justify-between gap-4 mb-4">
              <div>
                <p class="text-white text-sm mb-1">{concert.date} · {concert.time}</p>
                <h3 class="text-2xl md:text-3xl font-light text-white group-hover:text-muted transition-colors">
                  {concert.title}
                </h3>
              </div>
              <span class="flex-shrink-0 px-4 py-2 border border-white/30 text-white text-sm tracking-wider uppercase group-hover:bg-white group-hover:text-black transition-all">
                {concert.buttonText} →
              </span>
            </div>
            <p class="text-muted text-sm leading-relaxed">{concert.description}</p>
          </a>
          
          <!-- Остальные — компактно -->
          <div class="grid md:grid-cols-2 gap-4">
            <a href="#" class="group block p-4 border border-white/10 hover:border-white/30 transition-all no-underline">
              <p class="text-muted text-xs mb-1">{release.date}</p>
              <h4 class="text-white font-light group-hover:text-muted transition-colors">{release.title}</h4>
              <p class="text-muted text-xs mt-1">{release.buttonText} →</p>
            </a>
            <a href="#" class="group block p-4 border border-white/10 hover:border-white/30 transition-all no-underline">
              <p class="text-muted text-xs mb-1">{news.date}</p>
              <h4 class="text-white font-light group-hover:text-muted transition-colors">{news.title}</h4>
              <p class="text-muted text-xs mt-1">{news.buttonText} →</p>
            </a>
          </div>
          
          <p class="text-muted text-sm text-center mt-8">Все события →</p>
        </div>
      </section>

      <!-- ========== ВАРИАНТ B ========== -->
      <section class="mb-20">
        <div class="text-xs text-amber-400 mb-4 tracking-widest uppercase">Вариант B — Концерт с цветным акцентом</div>
        
        <div class="bg-surface py-12 px-6 -mx-6">
          <p class="text-muted text-sm tracking-[0.2em] uppercase mb-8 text-center">Актуальное</p>
          
          <!-- Концерт с акцентом -->
          <a href="#" class="group block mb-4 p-5 bg-white/5 border-l-4 border-amber-400 hover:bg-white/10 transition-all no-underline">
            <div class="flex items-center justify-between gap-4">
              <div>
                <p class="text-amber-400 text-xs uppercase tracking-wider mb-1">Концерт · {concert.date} · {concert.time}</p>
                <h3 class="text-xl font-light text-white group-hover:text-muted transition-colors">{concert.title}</h3>
                <p class="text-muted text-sm mt-1 line-clamp-1">{concert.description}</p>
              </div>
              <span class="flex-shrink-0 text-white text-sm flex items-center gap-2 group-hover:gap-3 transition-all">
                {concert.buttonText}
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                </svg>
              </span>
            </div>
          </a>
          
          <!-- Остальные -->
          <a href="#" class="group block mb-2 p-4 border border-white/10 hover:border-white/20 transition-all no-underline opacity-70 hover:opacity-100">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-muted text-xs mb-1">{release.date}</p>
                <h4 class="text-white font-light text-sm">{release.title}</h4>
              </div>
              <span class="text-muted text-xs">{release.buttonText} →</span>
            </div>
          </a>
          <a href="#" class="group block p-4 border border-white/10 hover:border-white/20 transition-all no-underline opacity-70 hover:opacity-100">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-muted text-xs mb-1">{news.date}</p>
                <h4 class="text-white font-light text-sm">{news.title}</h4>
              </div>
              <span class="text-muted text-xs">{news.buttonText} →</span>
            </div>
          </a>
          
          <p class="text-muted text-sm text-center mt-8">Все события →</p>
        </div>
      </section>

      <!-- ========== ВАРИАНТ C ========== -->
      <section class="mb-20">
        <div class="text-xs text-amber-400 mb-4 tracking-widest uppercase">Вариант C — Кнопка внизу карточки</div>
        
        <div class="bg-surface py-12 px-6 -mx-6">
          <p class="text-muted text-sm tracking-[0.2em] uppercase mb-8 text-center">Актуальное</p>
          
          <div class="space-y-4">
            <!-- Концерт -->
            <a href="#" class="group block p-5 border border-white/20 hover:border-white/40 hover:bg-white/5 transition-all no-underline">
              <p class="text-muted text-sm">{concert.date} · {concert.time}</p>
              <h3 class="text-xl font-light text-white group-hover:text-muted transition-colors mt-1">{concert.title}</h3>
              <p class="text-muted text-sm mt-2 line-clamp-2">{concert.description}</p>
              <div class="mt-4 pt-4 border-t border-white/10 flex items-center justify-between">
                <span class="text-xs text-amber-400 uppercase tracking-wider">Концерт</span>
                <span class="text-white text-sm flex items-center gap-2">
                  {concert.buttonText}
                  <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                  </svg>
                </span>
              </div>
            </a>
            
            <!-- Релиз -->
            <a href="#" class="group block p-5 border border-white/10 hover:border-white/20 transition-all no-underline opacity-70 hover:opacity-100">
              <p class="text-muted text-sm">{release.date}</p>
              <h3 class="text-lg font-light text-white group-hover:text-muted transition-colors mt-1">{release.title}</h3>
              <p class="text-muted text-sm mt-2 line-clamp-1">{release.description}</p>
              <div class="mt-3 flex items-center justify-between">
                <span class="text-xs text-green-400 uppercase tracking-wider">Релиз</span>
                <span class="text-muted text-sm">{release.buttonText} →</span>
              </div>
            </a>
            
            <!-- Новость -->
            <a href="#" class="group block p-5 border border-white/10 hover:border-white/20 transition-all no-underline opacity-70 hover:opacity-100">
              <p class="text-muted text-sm">{news.date}</p>
              <h3 class="text-lg font-light text-white group-hover:text-muted transition-colors mt-1">{news.title}</h3>
              <div class="mt-3 flex items-center justify-between">
                <span class="text-xs text-blue-400 uppercase tracking-wider">Новость</span>
                <span class="text-muted text-sm">{news.buttonText} →</span>
              </div>
            </a>
          </div>
          
          <p class="text-muted text-sm text-center mt-8">Все события →</p>
        </div>
      </section>

      <!-- ========== ВАРИАНТ D ========== -->
      <section class="mb-20">
        <div class="text-xs text-amber-400 mb-4 tracking-widest uppercase">Вариант D — Концерт отдельно, остальное списком</div>
        
        <div class="bg-surface py-12 px-6 -mx-6">
          <p class="text-muted text-sm tracking-[0.2em] uppercase mb-6 text-center">Ближайшее</p>
          
          <!-- Концерт — выделен -->
          <a href="#" class="group block mb-8 text-center no-underline">
            <p class="text-muted text-lg">{concert.date} · {concert.time}</p>
            <h3 class="text-3xl md:text-4xl font-light text-white group-hover:text-muted transition-colors mt-2" style="font-family: var(--font-logo);">
              {concert.title}
            </h3>
            <p class="text-muted mt-3 max-w-lg mx-auto">{concert.description}</p>
            <span class="inline-block mt-6 px-6 py-2 border border-white/30 text-white text-sm tracking-wider uppercase group-hover:bg-white group-hover:text-black transition-all">
              {concert.buttonText}
            </span>
          </a>
          
          <hr class="border-white/10 my-8" />
          
          <p class="text-muted text-sm tracking-[0.2em] uppercase mb-4 text-center">Также</p>
          
          <!-- Остальное списком -->
          <div class="space-y-3 max-w-md mx-auto">
            <a href="#" class="group flex items-center justify-between py-2 no-underline hover:bg-white/5 px-3 -mx-3 transition-all">
              <div>
                <span class="text-white">{release.title}</span>
                <span class="text-muted text-sm ml-2">{release.date}</span>
              </div>
              <span class="text-muted group-hover:text-white transition-colors">{release.buttonText} →</span>
            </a>
            <a href="#" class="group flex items-center justify-between py-2 no-underline hover:bg-white/5 px-3 -mx-3 transition-all">
              <div>
                <span class="text-white">{news.title}</span>
                <span class="text-muted text-sm ml-2">{news.date}</span>
              </div>
              <span class="text-muted group-hover:text-white transition-colors">{news.buttonText} →</span>
            </a>
          </div>
          
          <p class="text-muted text-sm text-center mt-8">Все события →</p>
        </div>
      </section>

      <!-- ========== ВАРИАНТ E ========== -->
      <section class="mb-20">
        <div class="text-xs text-amber-400 mb-4 tracking-widest uppercase">Вариант E — Минималистичный с иконками</div>
        
        <div class="bg-surface py-12 px-6 -mx-6">
          <p class="text-muted text-sm tracking-[0.2em] uppercase mb-8 text-center">Актуальное</p>
          
          <div class="space-y-1">
            <!-- Концерт -->
            <a href="#" class="group flex items-center gap-4 p-4 bg-white/5 hover:bg-white/10 transition-all no-underline">
              <div class="w-12 h-12 rounded-full bg-amber-400/20 flex items-center justify-center flex-shrink-0">
                <span class="text-amber-400 text-lg">🎫</span>
              </div>
              <div class="flex-1 min-w-0">
                <p class="text-white font-light group-hover:text-muted transition-colors">{concert.title}</p>
                <p class="text-muted text-sm">{concert.date} · {concert.time}</p>
              </div>
              <span class="text-white text-sm flex items-center gap-1 flex-shrink-0">
                {concert.buttonText}
                <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </span>
            </a>
            
            <!-- Релиз -->
            <a href="#" class="group flex items-center gap-4 p-4 hover:bg-white/5 transition-all no-underline opacity-70 hover:opacity-100">
              <div class="w-12 h-12 rounded-full bg-green-400/20 flex items-center justify-center flex-shrink-0">
                <span class="text-green-400 text-lg">🎵</span>
              </div>
              <div class="flex-1 min-w-0">
                <p class="text-white font-light group-hover:text-muted transition-colors">{release.title}</p>
                <p class="text-muted text-sm">{release.date}</p>
              </div>
              <span class="text-muted text-sm flex-shrink-0">{release.buttonText} →</span>
            </a>
            
            <!-- Новость -->
            <a href="#" class="group flex items-center gap-4 p-4 hover:bg-white/5 transition-all no-underline opacity-70 hover:opacity-100">
              <div class="w-12 h-12 rounded-full bg-blue-400/20 flex items-center justify-center flex-shrink-0">
                <span class="text-blue-400 text-lg">📰</span>
              </div>
              <div class="flex-1 min-w-0">
                <p class="text-white font-light group-hover:text-muted transition-colors">{news.title}</p>
                <p class="text-muted text-sm">{news.date}</p>
              </div>
              <span class="text-muted text-sm flex-shrink-0">{news.buttonText} →</span>
            </a>
          </div>
          
          <p class="text-muted text-sm text-center mt-8">Все события →</p>
        </div>
      </section>

    </div>
  </main>
</Layout>

<style>
  a {
    text-decoration: none !important;
  }
</style>


