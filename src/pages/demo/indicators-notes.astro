---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';

const lang = 'ru';
const itemCount = 6;
---

<Layout title="Демо: Нотный стан — варианты — Nomusicians">
  <Header currentLang={lang} />
  
  <main class="pt-24 pb-16">
    <section class="px-6 mb-16">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-4xl font-light tracking-wide mb-6" style="font-family: var(--font-logo);">
          Нотный стан
        </h1>
        <p class="text-lg text-muted">
          Минималистичные варианты
        </p>
      </div>
    </section>

    <!-- N1: Одна линия + точки -->
    <section class="mb-20 px-6">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-xl font-light tracking-wide mb-2">N1. Одна линия</h2>
        <p class="text-muted text-sm mb-8">Точки на/под линией</p>
        
        <div class="bg-surface/30 p-8 rounded-lg flex justify-center">
          <div class="relative">
            <div class="absolute top-1/2 left-0 right-0 h-[1px] bg-white/15"></div>
            <div class="relative flex gap-4 items-center">
              {Array.from({length: itemCount}).map((_, i) => {
                const above = [true, false, true, false, true, false];
                return (
                  <button 
                    class:list={[
                      "w-2 h-2 rounded-full transition-all duration-300",
                      i === 0 ? "bg-white" : "bg-white/30 hover:bg-white/50"
                    ]}
                    style={`transform: translateY(${above[i] ? '-6px' : '6px'})`}
                  />
                );
              })}
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- N2: Две линии -->
    <section class="mb-20 px-6">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-xl font-light tracking-wide mb-2">N2. Две линии</h2>
        <p class="text-muted text-sm mb-8">Точки между линиями и вне</p>
        
        <div class="bg-surface/30 p-8 rounded-lg flex justify-center">
          <div class="relative h-6">
            <div class="absolute top-0 left-0 right-0 h-[1px] bg-white/10"></div>
            <div class="absolute bottom-0 left-0 right-0 h-[1px] bg-white/10"></div>
            <div class="relative flex gap-5 items-center h-full px-2">
              {Array.from({length: itemCount}).map((_, i) => {
                const positions = [-4, 12, 4, 20, 8, 0]; // Разные Y
                return (
                  <button 
                    class:list={[
                      "w-1.5 h-1.5 rounded-full transition-all duration-300 absolute",
                      i === 0 ? "bg-white" : "bg-white/25 hover:bg-white/50"
                    ]}
                    style={`left: ${i * 24 + 8}px; top: ${positions[i]}px`}
                  />
                );
              })}
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- N3: Только точки на разных уровнях -->
    <section class="mb-20 px-6">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-xl font-light tracking-wide mb-2">N3. Без линий</h2>
        <p class="text-muted text-sm mb-8">Точки на разных высотах (подразумеваемый стан)</p>
        
        <div class="bg-surface/30 p-8 rounded-lg flex justify-center">
          <div class="relative h-6 flex gap-4 items-center">
            {Array.from({length: itemCount}).map((_, i) => {
              const y = [0, 10, 4, 14, 8, 2]; // Как ноты
              return (
                <button 
                  class:list={[
                    "w-2 h-2 rounded-full transition-all duration-300",
                    i === 0 ? "bg-white" : "bg-white/30 hover:bg-white/50"
                  ]}
                  style={`transform: translateY(${y[i] - 7}px)`}
                />
              );
            })}
          </div>
        </div>
      </div>
    </section>

    <!-- N4: Вертикальные штрихи (как такты) -->
    <section class="mb-20 px-6">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-xl font-light tracking-wide mb-2">N4. Тактовые линии</h2>
        <p class="text-muted text-sm mb-8">Вертикальные штрихи как разделители тактов</p>
        
        <div class="bg-surface/30 p-8 rounded-lg flex justify-center">
          <div class="flex gap-3 items-center h-4">
            {Array.from({length: itemCount}).map((_, i) => (
              <button 
                class:list={[
                  "w-[1px] h-full transition-all duration-300",
                  i === 0 ? "bg-white h-5" : "bg-white/20 hover:bg-white/40"
                ]}
              />
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- N5: Ноты-овалы -->
    <section class="mb-20 px-6">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-xl font-light tracking-wide mb-2">N5. Овальные ноты</h2>
        <p class="text-muted text-sm mb-8">Форма нот (овал вместо круга)</p>
        
        <div class="bg-surface/30 p-8 rounded-lg flex justify-center">
          <div class="relative h-6 flex gap-4 items-center">
            {Array.from({length: itemCount}).map((_, i) => {
              const y = [0, 8, 4, 12, 6, 2];
              return (
                <button 
                  class:list={[
                    "w-3 h-2 rounded-full transition-all duration-300 -rotate-12",
                    i === 0 ? "bg-white" : "bg-white/25 hover:bg-white/50"
                  ]}
                  style={`transform: translateY(${y[i] - 6}px) rotate(-20deg)`}
                />
              );
            })}
          </div>
        </div>
      </div>
    </section>

    <!-- N6: Минимал с линией -->
    <section class="mb-20 px-6">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-xl font-light tracking-wide mb-2">N6. Ультра-минимал</h2>
        <p class="text-muted text-sm mb-8">Одна тонкая линия, точки чуть выше/ниже</p>
        
        <div class="bg-surface/30 p-8 rounded-lg flex justify-center">
          <div class="relative">
            <div class="absolute top-1/2 left-0 right-0 h-[1px] bg-white/10"></div>
            <div class="relative flex gap-6 items-center">
              {Array.from({length: itemCount}).map((_, i) => {
                const up = i % 2 === 0;
                return (
                  <button 
                    class:list={[
                      "w-1.5 h-1.5 rounded-full transition-all duration-300",
                      i === 0 ? "bg-white" : "bg-white/25 hover:bg-white/50"
                    ]}
                    style={`transform: translateY(${up ? '-4px' : '4px'})`}
                  />
                );
              })}
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- N7: Пустые/заполненные ноты -->
    <section class="mb-20 px-6">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-xl font-light tracking-wide mb-2">N7. Заполненные/пустые</h2>
        <p class="text-muted text-sm mb-8">Активная нота заполнена, остальные — контур</p>
        
        <div class="bg-surface/30 p-8 rounded-lg flex justify-center">
          <div class="relative h-6 flex gap-4 items-center">
            {Array.from({length: itemCount}).map((_, i) => {
              const y = [0, 10, 4, 14, 8, 2];
              return (
                <button 
                  class:list={[
                    "w-2 h-2 rounded-full transition-all duration-300",
                    i === 0 
                      ? "bg-white" 
                      : "bg-transparent border border-white/30 hover:border-white/60"
                  ]}
                  style={`transform: translateY(${y[i] - 7}px)`}
                />
              );
            })}
          </div>
        </div>
      </div>
    </section>

    <!-- N8: Штили (ножки нот) -->
    <section class="mb-20 px-6">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-xl font-light tracking-wide mb-2">N8. Со штилями</h2>
        <p class="text-muted text-sm mb-8">Точки с вертикальной линией — как настоящие ноты</p>
        
        <div class="bg-surface/30 p-8 rounded-lg flex justify-center">
          <div class="relative h-8 flex gap-5 items-end">
            {Array.from({length: itemCount}).map((_, i) => {
              const heights = [20, 28, 24, 32, 22, 26];
              return (
                <div class="relative flex flex-col items-center">
                  <div 
                    class:list={[
                      "w-[1px] transition-all duration-300",
                      i === 0 ? "bg-white" : "bg-white/20"
                    ]}
                    style={`height: ${heights[i] - 8}px`}
                  />
                  <button 
                    class:list={[
                      "w-2 h-2 rounded-full transition-all duration-300 -mt-1",
                      i === 0 ? "bg-white" : "bg-white/30 hover:bg-white/50"
                    ]}
                  />
                </div>
              );
            })}
          </div>
        </div>
      </div>
    </section>

    <!-- N9: Горизонтальный ритм -->
    <section class="mb-20 px-6">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-xl font-light tracking-wide mb-2">N9. Ритм</h2>
        <p class="text-muted text-sm mb-8">Разный интервал между точками (как ритм)</p>
        
        <div class="bg-surface/30 p-8 rounded-lg flex justify-center">
          <div class="flex items-center">
            {Array.from({length: itemCount}).map((_, i) => {
              const gaps = [8, 16, 8, 24, 12, 8]; // Разные интервалы
              return (
                <>
                  <button 
                    class:list={[
                      "w-2 h-2 rounded-full transition-all duration-300",
                      i === 0 ? "bg-white" : "bg-white/30 hover:bg-white/50"
                    ]}
                  />
                  {i < itemCount - 1 && <div style={`width: ${gaps[i]}px`}></div>}
                </>
              );
            })}
          </div>
        </div>
      </div>
    </section>

    <!-- N10: Самый минимал -->
    <section class="mb-20 px-6">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-xl font-light tracking-wide mb-2">N10. Точки + одна акцентная линия</h2>
        <p class="text-muted text-sm mb-8">Почти как обычные точки, но с намёком на стан</p>
        
        <div class="bg-surface/30 p-8 rounded-lg flex justify-center">
          <div class="relative">
            <div class="absolute top-1/2 left-0 right-0 h-[1px] bg-white/5"></div>
            <div class="relative flex gap-3 items-center">
              {Array.from({length: itemCount}).map((_, i) => (
                <button 
                  class:list={[
                    "w-2 h-2 rounded-full transition-all duration-300",
                    i === 0 ? "bg-white" : "bg-white/25 hover:bg-white/45"
                  ]}
                />
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Рекомендация -->
    <section class="px-6 py-16 bg-surface/30">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-2xl font-light tracking-wide mb-8">Мои фавориты</h2>
        <div class="flex justify-center gap-8 flex-wrap">
          <div class="text-center">
            <div class="text-4xl mb-2">N3</div>
            <p class="text-sm text-muted">Без линий</p>
          </div>
          <div class="text-center">
            <div class="text-4xl mb-2">N6</div>
            <p class="text-sm text-muted">Ультра-минимал</p>
          </div>
          <div class="text-center">
            <div class="text-4xl mb-2">N7</div>
            <p class="text-sm text-muted">Контуры</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Навигация -->
    <section class="px-6 py-16 text-center space-x-4">
      <a href="/demo/indicators" class="text-white/60 hover:text-white transition-colors">
        ← Все индикаторы
      </a>
      <a href="/demo/galleries" class="text-white/60 hover:text-white transition-colors">
        Галереи →
      </a>
    </section>
  </main>
</Layout>

